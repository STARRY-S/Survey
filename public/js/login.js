"use strict"

const loginButton = document.getElementById("button-login");

const loginClicked = (username, password) => {
  ;
};

window.onclick = (event) {
  switch (event.loginButton) {
    case modal:

      break;
    default:

  };
}
